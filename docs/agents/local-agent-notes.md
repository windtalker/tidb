# Local Agent Notes

- Do not run `make bazel_lint_changed` proactively; only run it when the user explicitly asks.
- For failpoint enable/disable, use `tools/bin/failpoint-ctl` (not a system/global binary).
- Do not commit `docs/agents/local-agent-notes.md`.
